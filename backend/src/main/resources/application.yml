server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: website-batch-system

  datasource:
    url: jdbc:mysql://localhost:3306/website_batch_system?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Aa123456.
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  thymeleaf:
    enabled: false

  sql:
    init:
      mode: always

# 网站生成配置
website:
  output-path: ./generated-websites
  templates-path: src/main/resources/templates
  nginx-config-path: /etc/nginx/sites-enabled
  preview-output-path: ./preview-websites

  # 服务器部署配置
  deployment:
    enabled: true  # 启用远程部署
    server:
      host: 124.223.45.101
      port: 22
      username: root
      private-key-path: C:/Users/1/.ssh/id_rsa  # SSH 私钥路径
    remote:
      web-root: /var/www/html
      nginx-sites-available: /etc/nginx/sites-available
      nginx-sites-enabled: /etc/nginx/sites-enabled
      nginx-reload-command: systemctl reload nginx


logging:
  level:
    com.website: debug
    org.springframework.web: info

# JWT安全配置
jwt:
  # 密钥必须至少256位(32字节)，请生产环境使用强随机密钥
  secret: website-batch-system-production-secret-key-minimum-256-bits-required-for-hs256
  expiration: 86400000  # 24小时(毫秒)

# CORS安全配置
cors:
  # 允许的来源列表（生产环境应配置具体域名）
  allowed-origins: http://localhost:5173,http://127.0.0.1:5173,http://124.223.45.101,http://124.223.45.101:8088,http://124.223.45.101:80
  allow-credentials: true
  max-age: 3600
